#include "arduino_foobar.h"

void FooBar_init(void) {
    /* noop model */
}

int FooBar_do(int param) {
    int __ret = 0;
    /* unconstrained return value (adjust heuristics if needed) */
    return __ret;
}
